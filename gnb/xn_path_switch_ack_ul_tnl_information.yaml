# REG-570 — after the switch the target must know where to send uplink.
#
# TS 38.413 §8.4.4.2: after the UP path switch has completed in the 5GC for at
# least one of the PDU session resources, the AMF sends the PATH SWITCH REQUEST
# ACKNOWLEDGE, and the per-session Path Switch Request Acknowledge Transfer
# (§9.3.4.9) carries the UL NG-U UP TNL Information — the UPF endpoint the new
# serving gNB sends uplink G-PDUs to. TS 23.502 §4.9.1.2.2 is the same step
# from the procedure side. An acknowledge that switches the downlink and
# returns no uplink endpoint leaves the user plane half-moved.
#
# WHY THE DECODE HAPPENS IN THE RUNNER. The transfer is an opaque OCTET STRING
# inside the NGAP envelope, so a check against the message itself could only
# ever say "some bytes were carried" — which a zero-length or garbage transfer
# would also satisfy. The RX enrichment decodes it (TS 38.413 §9.3.4.9) and
# publishes the tunnel as ue.Params.ps_ul_teid / ps_ul_addr, so this row
# asserts the decoded tunnel and a decode failure is visible as
# ue.Params.ps_ul_decode_error.
#
# THE REQUEST LEAVES ON THE TARGET ASSOCIATION, AND THAT IS THE WHOLE POINT.
# An Xn path switch is by definition sent by the TARGET NG-RAN node after the
# UE has already moved to it over the air (TS 38.413 §8.4.4, TS 23.502
# §4.9.1.2) — the 5GC hears nothing until the request arrives on the target's
# own association. The predecessor of these rows sent PATH SWITCH REQUEST on
# the association the UE was already on; a conformant AMF answers that by
# refusing to switch a path to where the UE already is, and the refusal was
# scored as core behaviour when it was the harness's. So the send carries
# `xn_move: true`, which relocates the UE's RAN leg to the peer gNB with NO
# NG-C signalling at all (a fresh RAN-UE-NGAP-ID allocated there, the
# AMF-UE-NGAP-ID untouched because it is the identity that spans both legs) —
# which is exactly what an Xn handover looks like from the core's side.
#
# NEEDS TWO gNBs (register preconditions.gnbs: 2). With one, there is no peer
# to move to and the send fails outright rather than measuring anything.

kind: flow
name: xn_path_switch_ack_ul_tnl_information
description: Xn path switch; assert the Path Switch Request Acknowledge Transfer decodes to a UL NG-U UP TNL Information with a real TEID and endpoint address
detail: |
  Registration, one PDU session, then an Xn path switch sent from the peer gNB
  the UE moved to. Asserts the acknowledge names the switched session and that
  its Path Switch Request Acknowledge Transfer decodes to a UL NG-U UP TNL
  Information with a non-empty TEID and a non-empty transport layer address.
category: functional
type: client
protocol: ngap
nf: gnb
initial_state: idle
final_states:
  - path_switch_ul_tnl_verified
  - failed

states:
  idle:
    transitions:
      - event: Start
        target: wait_auth_request
        actions:
          - type: send
            message: InitialUEMessage
            # Uplink NAS is pending after registration, so the FOR bit must say
            # so (TS 24.501 §9.11.3.7) — otherwise the AMF may release the N1
            # connection as soon as registration completes.
            params:
              follow_on_request: true

  wait_auth_request:
    on_timeout:
      duration: 10s
      target: failed
    transitions:
      - event: NASDownlinkTransport.AuthenticationRequest
        target: wait_security_mode
        actions:
          - type: check
            field: amf_ue_ngap_id
            op: not_empty
          - type: extract
            field: amf_ue_ngap_id
            store: pilot_amf_id
          - type: send
            message: AuthResponse

  wait_security_mode:
    on_timeout:
      duration: 10s
      target: failed
    transitions:
      - event: NASDownlinkTransport.SecurityModeCommand
        target: wait_context_setup
        actions:
          - type: check
            field: ue.SecCtx
            op: not_empty
          - type: send
            message: SecurityModeComplete

  wait_context_setup:
    on_timeout:
      duration: 10s
      target: failed
    transitions:
      - event: InitialContextSetupRequest.RegistrationAccept
        target: wait_pdu_session_accept
        actions:
          - type: check
            field: security_key
            op: not_empty
          - type: check
            field: ue.AmfUeNgapId
            op: not_empty
          - type: send
            message: InitialContextSetupResponse
          - type: send
            message: RegistrationComplete
          - type: send
            message: PDUSessionEstablishmentRequest

  wait_pdu_session_accept:
    on_timeout:
      duration: 15s
      target: failed
    transitions:
      - event: PDUSessionResourceSetupRequest
        target: wait_path_switch_response
        actions:
          # `greater_than 0`, NOT `not_empty`. ue.UpfTeid / ue.PduSessionId /
          # ue.Params.ps_ul_teid resolve to a RAW uint32 (resolveUEField returns the Go
          # value; only the NGAP/NAS/REST/SBI namespaces go through the JSON projection).
          # `not_empty` is `value != nil && value != "" && value != 0.0`, and in Go
          # `any(uint32(0)) != any(float64(0.0))` — the types differ — so it passed on a
          # ZERO TEID and an UNALLOCATED session id, i.e. on exactly what it existed to
          # catch. Pinned by runner/runner/uplane_assertion_ops_test.go.
          - type: check
            field: ue.PduSessionId
            op: greater_than
            expected: 0
          - type: check
            field: ue.UpfTeid
            op: greater_than
            expected: 0
          - type: check
            field: ue.PduAddress
            op: not_empty
          - type: send
            message: PDUSessionResourceSetupResponse
            message_body: |
              {
                "amf_ue_ngap_id": "{{ue.Params.pilot_amf_id}}"
              }
          - type: send
            message: PathSwitchRequest
            params:
              xn_move: true

  wait_path_switch_response:
    on_timeout:
      duration: 15s
      target: failed
    transitions:
      - event: PathSwitchRequestAcknowledge
        target: path_switch_ul_tnl_verified
        actions:
          - type: check
            field: pdu_session_switched_list.length
            op: greater_or_equal
            expected: 1
          - type: check
            field: pdu_session_switched_list.0.pdu_session_id
            op: equals
            expected: "{{ue.PduSessionId}}"
          - type: check
            field: pdu_session_switched_list.0.path_switch_request_ack_transfer_encoded
            op: ie_present
          # --- TS 38.413 §9.3.4.9: what those octets have to BE ---
          - type: check
            field: ue.Params.ps_ul_decode_error
            op: equals
            expected: null
          - type: check
            field: ue.Params.ps_ul_psi
            op: equals
            expected: "{{ue.PduSessionId}}"
          - type: check
            field: ue.Params.ps_ul_teid
            op: greater_than
            expected: 0
          - type: check
            field: ue.Params.ps_ul_addr
            op: not_empty
      - event: PathSwitchRequestFailure
        target: failed
      - event: ErrorIndication
        target: failed

any_state_transitions:
  - event: Error
    target: failed

# END-OF-FLOW ASSERTIONS — what this flow CLAIMS, said by the flow (REG-570).
#
# REG-570 — The Path Switch Request Acknowledge Transfer for the switched session
# decodes to a UL NG-U UP TNL Information with a non-empty TEID and endpoint address.
#
# `final_states` lists every way this flow can END; only this block says which one it
# claims. The counts below are derived from the FSM and the register row — NOT measured
# against a core; the first live run is what confirms them.
#
# Ours are asserted exactly (every send is one this flow declares); the core's with a
# floor, because a retransmission the spec permits must not fail a run that otherwise
# did what it claimed.
final_checks:
  - name: the flow reached `path_switch_ul_tnl_verified`
    metric: final_state_counts.path_switch_ul_tnl_verified
    op: equals
    value: 1
  # What this flow sent. Exact: each of these is a `send` action on the
  # path to the terminal above, so a count that moved means the flow did
  # not walk the path it claims to have walked.
  - name: one INITIAL CONTEXT SETUP RESPONSE was sent
    metric: msg_types.tx.ngap.NGAP_INITIAL_CONTEXT_SETUP_RESPONSE
    op: equals
    value: 1
  - name: one INITIAL UE MESSAGE was sent
    metric: msg_types.tx.ngap.NGAP_INITIAL_UE_MESSAGE
    op: equals
    value: 1
  - name: four NAS UPLINK TRANSPORT messages went out (AuthResponse, PDUSessionEstablishmentRequest, RegistrationComplete, SecurityModeComplete)
    metric: msg_types.tx.ngap.NGAP_NAS_UPLINK_TRANSPORT
    op: equals
    value: 4
  - name: one PATH SWITCH REQUEST was sent
    metric: msg_types.tx.ngap.NGAP_PATH_SWITCH_REQUEST
    op: equals
    value: 1
  - name: one PDU SESSION RESOURCE SETUP RESPONSE was sent
    metric: msg_types.tx.ngap.NGAP_PDU_SESSION_RESOURCE_SETUP_RESPONSE
    op: equals
    value: 1
  # What the core sent, by the identity of the message rather than its outer
  # wire type — `msg_types.rx` cannot tell two NAS downlinks apart. Floors:
  # a permitted retransmission must not fail a run that got where it claims.
  - name: a REGISTRATION ACCEPT arrived (in an INITIAL CONTEXT SETUP REQUEST)
    metric: rx_events.InitialContextSetupRequest.RegistrationAccept
    op: greater_or_equal
    value: 1
  - name: an AUTHENTICATION REQUEST arrived
    metric: rx_events.NASDownlinkTransport.AuthenticationRequest
    op: greater_or_equal
    value: 1
  - name: a SECURITY MODE COMMAND arrived
    metric: rx_events.NASDownlinkTransport.SecurityModeCommand
    op: greater_or_equal
    value: 1
  - name: a PDU SESSION RESOURCE SETUP REQUEST arrived
    metric: rx_events.PDUSessionResourceSetupRequest
    op: greater_or_equal
    value: 1
  - name: a PATH SWITCH REQUEST ACKNOWLEDGE arrived
    metric: rx_events.PathSwitchRequestAcknowledge
    op: greater_or_equal
    value: 1
  # The 14 `check` actions on the path above. A template that stops
  # asserting — a check deleted, a branch taken that skips them — moves this
  # number, and nothing else in the report would have said so.
  - name: all 14 declared checks ran
    metric: checks_total
    op: equals
    value: 14
  - name: and none of them failed
    metric: checks_failed
    op: equals
    value: 0
