# N2 HandoverRequired with NO PDU session to relocate (3GPP TS 38.413 §8.4.1).
# Registers (no PDU session), then sends HandoverRequired. In a single-gNB lab
# the AMF cannot find a target and reports HandoverPreparationFailure /
# ErrorIndication — exercised here without any PDU session in the request.
kind: flow
name: n2_handover_required_no_pdu_session
description: HandoverRequired with no PDU session; assert graceful prep failure (YAML)
category: negative
type: client
protocol: ngap
nf: gnb
initial_state: idle
final_states: [prep_failed, handed_over, failed]
states:
  idle:
    transitions:
      - event: Start
        target: wait_auth_request
        actions: [{ type: send, message: InitialUEMessage }]
  wait_auth_request:
    on_timeout: { duration: 10s, target: failed }
    transitions:
      - event: NASDownlinkTransport.AuthenticationRequest
        target: wait_security_mode
        actions:
          - { type: check, field: amf_ue_ngap_id, op: not_empty }
          - { type: send, message: AuthResponse }
  wait_security_mode:
    on_timeout: { duration: 10s, target: failed }
    transitions:
      - event: NASDownlinkTransport.SecurityModeCommand
        target: wait_context_setup
        actions:
          - { type: check, field: ue.SecCtx, op: not_empty }
          - { type: send, message: SecurityModeComplete }
  wait_context_setup:
    on_timeout: { duration: 10s, target: failed }
    transitions:
      - event: InitialContextSetupRequest.RegistrationAccept
        target: wait_ho_reaction
        actions:
          - { type: send, message: InitialContextSetupResponse }
          - { type: send, message: RegistrationComplete }
          - { type: send, message: HandoverRequired }
  wait_ho_reaction:
    on_timeout: { duration: 12s, target: failed }
    transitions:
      - { event: HandoverPreparationFailure, target: prep_failed }
      - { event: ErrorIndication, target: prep_failed }
      - { event: HandoverCommand, target: handed_over }
any_state_transitions:
  - { event: Error, target: failed }

# END-OF-FLOW ASSERTIONS — the runner is the only thing that decides whether this flow
# ran correctly, so the row's claim is stated here rather than reassembled downstream.
#
# THE REGISTER'S CLAIM, VERBATIM: REG-504 (robustness, expected_terminal: null) — Within
# the timeout the source association receives HandoverPreparationFailure or
# ErrorIndication, no HandoverCommand appears, and no HANDOVER REQUEST is relayed to the
# second gNB's association; the SCTP association stays up and a subsequent uplink NAS
# message is still served.
#
# REG-504 declares `expected_terminal: null`; its criterion names one anyway: "Within
# the timeout the source association receives HandoverPreparationFailure or
# ErrorIndication, no HandoverCommand appears". `handed_over` is that HANDOVER COMMAND —
# a handover prepared from a HANDOVER REQUIRED with no PDU Session Resource List, an IE
# that is mandatory with criticality `reject`.
#
# Counts below are derived from this FSM's own transitions and from the clauses named
# above; no core was contacted. checks_total is a FLOOR, not an equality: several of
# these flows can loop through a waiting state, and a floor cannot manufacture a failure
# the way an equality can.
final_checks:
  - name: the flow reached `prep_failed` — the terminal this row claims
    metric: final_state_counts.prep_failed
    op: equals
    value: 1
  - name: the run asserted something on the messages it saw
    metric: checks_total
    op: greater_or_equal
    value: 2
  - name: and no assertion failed
    metric: checks_failed
    op: equals
    value: 0
