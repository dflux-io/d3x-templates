# N2 HandoverRequired toward an UNKNOWN target gNB (3GPP TS 38.413 §8.4.1.4).
# Registers + establishes a PDU session, then sends HandoverRequired. In a
# single-gNB lab there is no target gNB, so the AMF returns
# HandoverPreparationFailure (or ErrorIndication "cannot find target gNB-id").
kind: flow
name: n2_handover_preparation_failure_unknown_target
description: HandoverRequired to a non-existent target; assert HandoverPreparationFailure (YAML)
category: negative
type: client
protocol: ngap
nf: gnb
initial_state: idle
final_states: [prep_failed, handed_over, failed]
states:
  idle:
    transitions:
      - event: Start
        target: wait_auth_request
        actions: [{ type: send, message: InitialUEMessage, params: { follow_on_request: true } }]  # FOR bit: uplink NAS pending (TS 24.501 §9.11.3.7)
  wait_auth_request:
    on_timeout: { duration: 10s, target: failed }
    transitions:
      - event: NASDownlinkTransport.AuthenticationRequest
        target: wait_security_mode
        actions:
          - { type: check, field: amf_ue_ngap_id, op: not_empty }
          - { type: extract, field: amf_ue_ngap_id, store: pilot_amf_id }
          - { type: send, message: AuthResponse }
  wait_security_mode:
    on_timeout: { duration: 10s, target: failed }
    transitions:
      - event: NASDownlinkTransport.SecurityModeCommand
        target: wait_context_setup
        actions:
          - { type: check, field: ue.SecCtx, op: not_empty }
          - { type: send, message: SecurityModeComplete }
  wait_context_setup:
    on_timeout: { duration: 10s, target: failed }
    transitions:
      - event: InitialContextSetupRequest.RegistrationAccept
        target: wait_pdu_session_accept
        actions:
          - { type: send, message: InitialContextSetupResponse }
          - { type: send, message: RegistrationComplete }
          - { type: send, message: PDUSessionEstablishmentRequest }
  wait_pdu_session_accept:
    on_timeout: { duration: 15s, target: failed }
    transitions:
      - event: PDUSessionResourceSetupRequest
        target: wait_ho_reaction
        actions:
          # `greater_than 0`, NOT `not_empty`. ue.UpfTeid / ue.PduSessionId /
          # ue.Params.ps_ul_teid resolve to a RAW uint32 (resolveUEField returns the Go
          # value; only the NGAP/NAS/REST/SBI namespaces go through the JSON projection).
          # `not_empty` is `value != nil && value != "" && value != 0.0`, and in Go
          # `any(uint32(0)) != any(float64(0.0))` — the types differ — so it passed on a
          # ZERO TEID and an UNALLOCATED session id, i.e. on exactly what it existed to
          # catch. Pinned by runner/runner/uplane_assertion_ops_test.go.
          - { type: check, field: ue.PduSessionId, op: greater_than, expected: 0 }
          - type: send
            message: PDUSessionResourceSetupResponse
            message_body: |
              { "amf_ue_ngap_id": "{{ue.Params.pilot_amf_id}}" }
          - { type: send, message: HandoverRequired }
  wait_ho_reaction:
    on_timeout: { duration: 12s, target: failed }
    transitions:
      - { event: HandoverPreparationFailure, target: prep_failed }
      - { event: ErrorIndication, target: prep_failed }
      - { event: HandoverCommand, target: handed_over }
any_state_transitions:
  - { event: Error, target: failed }

# END-OF-FLOW ASSERTIONS — the runner is the only thing that decides whether this flow
# ran correctly, so the row's claim is stated here rather than reassembled downstream.
#
# THE REGISTER'S CLAIM, VERBATIM: REG-505 (negative, expected_terminal: prep_failed) —
# HandoverPreparationFailure is received on the source association with a Cause IE
# present, or an ErrorIndication is received; the UE remains served by the source gNB
# and an uplink NAS message still gets a response.
#
# REG-505: "HandoverPreparationFailure is received on the source association with a
# Cause IE present, or an ErrorIndication is received." `handed_over` is a HANDOVER
# COMMAND for a target the 5GC cannot reach, which is the finding.
#
# Counts below are derived from this FSM's own transitions and from the clauses named
# above; no core was contacted. checks_total is a FLOOR, not an equality: several of
# these flows can loop through a waiting state, and a floor cannot manufacture a failure
# the way an equality can.
final_checks:
  - name: the flow reached `prep_failed` — the terminal this row claims
    metric: final_state_counts.prep_failed
    op: equals
    value: 1
  - name: the run asserted something on the messages it saw
    metric: checks_total
    op: greater_or_equal
    value: 3
  - name: and no assertion failed
    metric: checks_failed
    op: equals
    value: 0
